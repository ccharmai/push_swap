Создать голову стека со значением 5
	t_stack *head = create_head(5)

Высвободить память, выделененную под стек:
	free_stack(stack);

Создать стек из массива интов:
	int mass[] = {4, 5, 6, 7, 8, 3, 3, 2, 5, 6};
	int len_mass = 10;
	t_stack	*stack = init_stack_with_mass(len_mass,  mass);

Распечатать стек:
	print_stack(stack);

Удалить элемент под номером i из стека, при этом получив его значение:
	int k = delete_element(&stack, i)
	Если i не будет выходить за пределы стека, то удалится последний элемент
	Аналогично можно удалить и первый элемент без изменения головы
